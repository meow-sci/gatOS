# gatOS trimmed initramfs (OS_PLAN.md T2.4): direct kernel boot of a partitionless
# ext4 virtio disk needs exactly these features — drop ata/ide/scsi/usb/raid/etc.
# build-image.sh re-runs mkinitfs with this config after the overlay is applied.
features="base virtio ext4"

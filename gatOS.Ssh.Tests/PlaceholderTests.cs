namespace gatOS.Ssh.Tests;

/// <summary>
///     Placeholder so <c>gatOS.Ssh.Tests</c> participates in the suite before real Ssh tests
///     arrive (OS_PLAN.md T0.1). Delete when the first real fixture is added.
/// </summary>
[TestFixture]
public sealed class PlaceholderTests
{
    [Test]
    public void Placeholder() => Assert.Pass();
}

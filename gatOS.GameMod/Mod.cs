namespace gatOS.GameMod;

/// <summary>
///     Entry point skeleton for the gatOS KSA mod. The StarMap lifecycle hooks (mod init,
///     <c>OnBeforeGui</c> telemetry sampling), custom-shell registration with purrTTY's
///     <c>CustomShellRegistry</c>, the diagnostics menu and dist packaging are implemented at
///     milestone M6 (OS_PLAN.md). This placeholder keeps the assembly buildable in the M0
///     scaffold; delete the placeholder body when M6 lands.
/// </summary>
internal static class Mod
{
}

namespace gatOS.SimFs.Tests;

/// <summary>
///     Placeholder so <c>gatOS.SimFs.Tests</c> participates in the suite before real SimFs tests
///     arrive (OS_PLAN.md T0.1). Delete when the first real fixture is added.
/// </summary>
[TestFixture]
public sealed class PlaceholderTests
{
    [Test]
    public void Placeholder() => Assert.Pass();
}
